@use "../../task-item/taskItem.module.scss" as taskItem;

.projectTaskItem {
  @extend .taskItem;
  box-shadow: 0 2px 2px 2px rgba($color: #000000, $alpha: 0.3);
  padding: 0.8em 0.6em;
  h3 {
    font-size: 0.6rem;
    font-weight: 300;
  }

  button {
    margin-right: 1em;
    @extend .doneBtn;
  }
  // border-right: 10px solid ;
}
